
#akka related configurations

akka {
    loglevel = DEBUG,
    log-config-on-start = off,
    actor {
      provider = "akka.remote.RemoteActorRefProvider"
    }
    remote {
      enabled-transports = ["akka.remote.netty.tcp"]
      netty.tcp {
        hostname = "127.0.0.1"
        port = 2557
      }
    },
    debug{
     lifecycle = on
    }
  }

db :{
	default : {
	url    					:"jdbc:mysql://localhost/jsc?user=jsc&password=jsc1234"
	profile :{driver                  :"scala.slick.driver.MySQLDriver"}
	driver             :"com.mysql.jdbc.Driver"
	}
}

proversity.billing.system.context=Billing
include "../../../../../modules/model/src/main/resources/application.conf"
